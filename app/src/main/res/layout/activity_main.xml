<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorPrimary"
    android:paddingTop="60dp"
    tools:context=".MainActivity">

    <FrameLayout
        android:id="@+id/container_layout"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <pro.midev.expandedmenulibrary.ExpandedMenuView
        android:id="@+id/nav_view"
        android:layout_width="0dp"
        android:layout_height="100dp"
        app:em_background_color="@color/colorAccent"
        app:em_close_menu_icon="@drawable/ic_close_dark"
        app:em_is_on_click_closable="true"
        app:em_menu_icon="@drawable/ic_menu_dark"
        app:em_outside_margin="24dp"
        app:em_shadow_color="@android:color/black"
        app:em_text_color="@color/colorPrimary"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="@+id/container_layout"
        app:layout_constraintStart_toStartOf="@+id/container_layout"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="1.0" />

</androidx.constraintlayout.widget.ConstraintLayout>